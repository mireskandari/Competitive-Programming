#include <bits/stdc++.h>
using namespace std;
using ll = long long;

signed main() {
    int tc;
    scanf("%d", &tc);
    while (tc--) {
        int n;
        scanf("%d", &n);
        printf("%d\n", (n / 2) + 1);
    }
    return 0;
}
